# Архитектура

Semantic News Novelty состоит из HTTP-приложения, асинхронного ML-воркера,
хранилища, очереди и пользовательского интерфейса.

```text
Пользователь / интеграция
          │
        nginx
       ┌──┴───┐
       │      │
  Streamlit  FastAPI
                 │
        ┌────────┴────────┐
        │                 │
   PostgreSQL          RabbitMQ
   + pgvector              │
        ▲        model-service-vectorizer
        │        model-service-processor
        └───────────────────────────────┘
```

## Компоненты

- `nginx` — единая HTTP-точка входа;
- `ui` — Streamlit-интерфейс для работы с публикациями и поиском;
- `api` — пользователи, организации, импорт, публикация, учёт операций и задачи;
- `rabbitmq` — очереди заданий поиска, векторизации и aggregate;
- `model-service-vectorizer-*` — BGE-M3 embeddings на GPU или CPU;
- `model-service-processor` — кластеризация, классификация новизны и сохранение результатов;
- `postgres` — статьи, задания, эмбеддинги и состояние пайплайна;
- `pgvector` — cosine search по векторам размерности 1024.

Актуальная runtime-конфигурация ML-части одна: BGE-M3 embeddings, baseline+attach
кластеризация и CatBoost-классификатор новизны на 18 previous-only признаках.
Альтернативные наборы признаков и модели остаются историей экспериментов в
ноутбуках, но не входят в сервисный контур.

## Поток публикации

1. Publisher создаёт черновик или импортирует файл.
2. Публикация переводит статьи из `draft` в `public`.
3. API создаёт задачу и отправляет в RabbitMQ только ID статей и режим.
4. `model-service-vectorizer-*` читает содержимое из PostgreSQL и сохраняет embeddings.
5. `model-service-processor` читает embeddings и историю, затем рассчитывает кластеры и новизну.
6. Результат и версии моделей сохраняются в одной транзакции.
7. Обработанные публичные статьи становятся доступны поиску.

Model-service containers не загружают тексты статей из внешней сети.

Для больших incremental-пачек API создаёт parent job, несколько child jobs
`vectorize` и набор отложенных child jobs `aggregate`. Входные ID сначала
сортируются по `published_at`, чтобы aggregate-пачки шли в хронологическом
порядке. GPU-воркеры считают embeddings и сохраняют их в
`article_pipeline_embeddings`; после готовности всех embeddings CPU-воркеры
последовательно забирают aggregate-пачки из отдельной очереди.

## Очереди и поиск

Поисковые запросы публикуются в `NEWS_SEARCH_QUEUE`, а bulk-векторизация статей
в `NEWS_VECTORIZATION_QUEUE`. Vectorizer читает search-очередь первой, поэтому
новый поиск не ждёт весь backlog массовой векторизации. Уже начатый chunk не
прерывается: поиск стартует после завершения текущей задачи.

## Состояния

Видимость и обработка независимы:

```text
visibility: draft | public | archived
processing: not_started | pending | processing | processed | error
```

Задача проходит состояния:

```text
queued → processing → done
                    ↘ failed
```

## Хранение ML-состояния

Для статьи сохраняются:

- BGE-M3 embedding и ревизия модели;
- `cluster_id` и происхождение назначения;
- `novelty_label` и `p_significant`;
- признаки неоднозначности и позднего поступления;
- версии пайплайна, конфигурации и классификатора.

Runtime не использует FAISS. Векторы и HNSW-индекс находятся в
PostgreSQL/pgvector.

Основным источником истины для инфоповодов остаются
`article_pipeline_state.cluster_id` и `news_cluster_summaries`. Отдельный
справочник источников новостей в MVP не вводится: публикации привязываются к
организации, пользователю, URL и хешу содержимого.

## Полный и инкрементальный режимы

`full` совместно пересчитывает выбранный корпус и подходит для первичной
загрузки или периодического reclustering.

`incremental` сохраняет историю и последовательно добавляет новые публикации.
Он подходит для небольших поступающих батчей, но накапливает расхождение с
полным пересчётом.

Если количество ID в incremental job больше `PIPELINE_CHUNK_SIZE`, стадия
векторизации разбивается на подзадачи. После завершения всех vectorize-подзадач
aggregate выполняется пачками `PIPELINE_AGGREGATE_BATCH_SIZE`. Следующая
aggregate-пачка публикуется только после завершения предыдущей, поэтому
хронологический порядок сохраняется даже при нескольких CPU-воркерах.
Такой порядок сохраняет детерминированность результата для большого импорта и
уменьшает длительность отдельной RabbitMQ delivery. Для более потоковой
обработки можно будет перейти к временным окнам: отправлять готовые
vectorize-пачки в кластеризацию по мере готовности, но фиксировать watermark по
`published_at`, чтобы classifier видел только допустимый предшествующий
контекст и не менял смысл признаков новизны.

При обработке aggregate история загружается не целиком, а по датам текущей пачки
с запасом `PIPELINE_HISTORY_WINDOW_DAYS` дней в обе стороны. Это переносит
существующие временные ограничения ближе к PostgreSQL и не заставляет pandas
сканировать историю, которая всё равно не попадёт в baseline/attach окна.

Подробности находятся в [ML-пайплайне](ml_pipeline.md).

## Границы ответственности

- API отвечает за доступ, жизненный цикл статьи и постановку задач.
- Model-service containers отвечают за ML-вычисления, но не за права пользователя.
- PostgreSQL является источником истины для статей и результатов.
- RabbitMQ доставляет задания, но не хранит итоговое состояние продукта.
- UI использует публичный API и не обращается к базе напрямую.

## Связанные документы

- [Быстрый запуск](getting_started.md)
- [HTTP API](api.md)
- [Развёртывание](deployment.md)
- [ML-пайплайн](ml_pipeline.md)
