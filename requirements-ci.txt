# Minimal dependencies for GitHub Actions unit tests.
# Heavy ML dependencies from requirements.txt are intentionally excluded.
fastapi
pydantic-settings
aio-pika
SQLAlchemy
psycopg[binary]
pytest
httpx

# Lightweight data/ML stack required by final pipeline unit tests.
numpy
pandas
scikit-learn
